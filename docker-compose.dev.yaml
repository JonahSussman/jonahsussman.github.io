services:
  nginx:
    ports:
      - "8080:80"
    volumes:
      # Replace entire conf.d with just the dev config
      - ./nginx/conf.d/dev.conf:/etc/nginx/conf.d/default.conf:ro
      - /dev/null:/etc/nginx/conf.d/dev.conf:ro
      - /dev/null:/etc/nginx/ssl:ro
